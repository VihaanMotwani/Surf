# === REQUIRED ===
OPENAI_API_KEY=your_openai_api_key_here
OPENAI_MODEL=gpt-4.1-mini

# Browser-Use cloud API key (get one at https://browser-use.com)
BROWSER_USE_API_KEY=your_browser_use_api_key_here

# === OPTIONAL ===
# SQLite database path (auto-created on first run)
DATABASE_URL=sqlite+aiosqlite:///./surf.db

# Task polling
TASK_POLL_INTERVAL=2.0

# Zep Cloud memory (optional — app works without it)
ZEP_API_KEY=your_zep_api_key_here
ZEP_USER_ID=surf_local_user
ZEP_USER_NAME=User
